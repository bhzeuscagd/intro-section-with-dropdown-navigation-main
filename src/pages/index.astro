---
/**
 * @page Index
 * @description
 * [EN] The landing page of the application.
 * Composes the layout using the `Layout` wrapper and stacks the high-level sections (`Header`, `HeroSection`).
 *
 * [ES] La página de aterrizaje de la aplicación.
 * Compone el diseño usando el envoltorio `Layout` y apila las secciones de alto nivel (`Header`, `HeroSection`).
 */
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header/Header.astro";
import HeroSection from "../components/Hero/HeroSection.astro";
---

<Layout>
	<Header />
	<HeroSection />
</Layout>;
